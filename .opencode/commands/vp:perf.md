---
description: 分析并优化构建性能
agent: build
---

分析 VitePress 构建性能并提供优化建议。

执行步骤：

1. 运行 `pnpm docs:build` 并记录时间
2. 检查构建产物大小
3. 分析以下优化点：
   - 图片是否优化（WebP、尺寸）
   - 是否启用懒加载
   - 大型组件是否使用异步加载
   - 全局组件数量是否过多
   - Vite 插件是否影响性能
4. 提供具体的优化建议

如果用户同意，自动应用安全的优化。

示例：`/vp:perf`
